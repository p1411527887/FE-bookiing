version: "3.8"
services:
  nginx:
    build:
      context: .
    ports:
      - 80:80 
    volumes:
      - /home/hongphat/booking care/full-source-video35-Fullstack-HoiDanIT/FE-react/build:/usr/share/nginx/html
      - ./default.conf:/etc/nginx/conf.d/default.conf
